import { TitleStringDisplayForGradingPipe } from './title-string-display-for-grading.pipe';

describe('TitleStringDisplayForGradingPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleStringDisplayForGradingPipe();
    expect(pipe).toBeTruthy();
  });
});
